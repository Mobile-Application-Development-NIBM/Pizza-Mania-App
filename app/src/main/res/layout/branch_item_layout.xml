<?xml version="1.0" encoding="utf-8"?>
<!-- LinearLayout: main vertical container -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <!-- LinearLayout: row for category title and spacer -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- TextView: displays category/section title (e.g., Branch) -->
        <TextView
            android:id="@+id/categoryName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Branch"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#000000" />

        <!-- Space: pushes following views to right by filling remaining width -->
        <Space
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />
        <!-- layout_weight="1": makes space expand and push content -->
    </LinearLayout>

    <!-- RecyclerView: horizontal list of items under this category -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/menuRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="8dp"
        android:scrollbars="none" />
    <!-- orientation="horizontal": items scroll left to right -->
    <!-- scrollbars="none": hides scrollbar for cleaner UI -->
</LinearLayout>
